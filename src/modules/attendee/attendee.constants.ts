export enum AttendeeRsvpStatus {
  PENDING = 'pending',
  CONFIRMED = 'confirmed',
  DECLINED = 'declined',
}

export enum AttendanceIntent {
  BOTH = 'BOTH',
  VOLUNTEER_ONLY = 'VOLUNTEER_ONLY',
  TRAINEE_ONLY = 'TRAINEE_ONLY',
  NONE = 'NONE',
}
